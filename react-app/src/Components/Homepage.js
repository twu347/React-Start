import React from 'react';

function HomePage(){
    return(
        <div>
            <h1>This is shown inside Homepage React Component</h1>
            <h1>This component is used to show home page interface </h1>
        </div>
    )
};

export default HomePage;